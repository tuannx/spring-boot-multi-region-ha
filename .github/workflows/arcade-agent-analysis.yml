name: Arcade Architecture Analysis

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  architecture-analysis:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Run arcade-agent
        # v0.1.1
        uses: lemduc/arcade-agent/actions/analyze@8090d12f5139aa2dd969405a2d2326551c1ecb2a
        with:
          arcade-agent-version: "0.1.1"
          source-path: app/src/main/java
          language: java
          repo-name: spring-boot-multi-region-ha
          primary-algorithm: pkg
          run-secondary-analyses: "true"
          wca-num-clusters: "4"
